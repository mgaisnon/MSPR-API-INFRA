name: Deploy Global Stack

on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
      - 'feature/**'
      - 'hotfix/**'
  pull_request:
    branches:
      - main
      - develop
      - 'release/**'
      - 'feature/**'
      - 'hotfix/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout infra
        uses: actions/checkout@v4

      - name: Docker Compose Up
        run: |
          docker compose pull
          make up

      - name: Vérification des routes Traefik
        run: |
          # curl -f http://localhost/clients/health || (echo "Clients API failed" && exit 1)
          # curl -f http://localhost/produits/health || (echo "Produits API failed" && exit 1)
          # curl -f http://localhost/commandes/health || (echo "Commandes API failed" && exit 1)

          echo "🔎 Vérification des services techniques..."
          curl -f http://rabbitmq.localhost              || (echo "::error ::RabbitMQ UI not available" && exit 1)
          curl -f http://prometheus.localhost       || (echo "::error ::Prometheus not ready" && exit 1)
          curl -f http://grafana.localhost    || (echo "::error ::Grafana not reachable" && exit 1)
          curl -f http://kibana.localhost    || (echo "::error ::Kibana not reachable" && exit 1)
          curl -f http://traefik.localhost    || (echo "::error ::Traefik not reachable" && exit 1)